#!/usr/bin/env bash

# If I wanted to make changes beyond the default OH MY Zsh Theme I am using.
# But I like the Theme I am currently using so, aucune bonne raison de le faire!!

# If I made changes I would add this back to the loop that loads the tiles in bash_profile